server {
    listen 1080;

    location /statics {
        alias /usr/share/nginx/html/;
        index index.html;
    }

    location / {
        return 403;  # Deny access to everything else
    }
}
